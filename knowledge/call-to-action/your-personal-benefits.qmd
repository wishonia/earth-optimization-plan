---
title: Your Personal Benefits
description: The personal upside of moving from the Failed-State Trajectory to the Minimum Sustainable Trajectory (1% Treaty) or the Optimal Governance Trajectory, plus a calculator for selfishly rational contribution sizing.
image: /assets/og-images/knowledge/call-to-action/your-personal-benefits-og-retro-academic.jpg
podcast-image: /assets/podcast/your-personal-benefits-podcast.jpg
youtube-thumbnail: /assets/podcast/your-personal-benefits-youtube.jpg
scores:
  quality: 4
  value: 5
  timeliness: 6
  standalone: 3
  voice: 4
  importance: 6
---
{{< include ../includes/setup-parameters.qmd >}}

Forget saving humanity. Price the future you personally have to live in.

The [Earth Optimization Prize](../strategy/earth-optimization-prize.qmd) [@earth-optimization-prize-paper-2025] is not mainly a story about whether a `PRIZE` deposit earns on the deposit itself. It is a story about what happens to the rest of your life if civilization keeps compounding toward extraction, or if it pivots toward health and productive growth instead.

## The Average Human On Earth

If you enter nothing into the calculator below, it starts from the model's default human:

| Baseline | Current modeled value |
|---|---|
| **Age** | {{< var global_median_age_2024 >}} |
| **Remaining life** | {{< var global_avg_remaining_years >}} |
| **Annual income** | {{< var global_avg_income_2025 >}} |
| **Annual savings capacity** | {{< var global_annual_savings_per_capita >}} |

That default is not "you." It is the fastest way to make the math legible before you personalize it.

## The Four Futures

| Future | What the model says |
|---|---|
| **Failed-State Trajectory** | The average person ends up with roughly {{< var current_trajectory_cumulative_lifetime_income >}} in cumulative lifetime income while the destructive economy rises from {{< var global_destructive_economy_pct_gdp >}} of GDP now, reaches 25% by {{< var destructive_economy_25pct_year >}}, and reaches 50% of GDP by {{< var destructive_economy_50pct_year >}}, where stealing beats creating because whatever gets built gets looted. |
| **Minimum Sustainable Trajectory (1% Treaty)** | Adds {{< var treaty_trajectory_lifetime_income_gain_per_capita >}} in lifetime income, {{< var treaty_hale_gain_year_15 >}} in healthy life by year 15, and {{< var treaty_personal_upside_blend >}} in blended upside. |
| **[Wishonian](../futures/wishonia.qmd) [Optimal Governance Trajectory](../economics/gdp-trajectories.qmd)** | Adds {{< var wishonia_trajectory_lifetime_income_gain_per_capita >}} in lifetime income, {{< var wishonia_hale_gain_year_15 >}} in healthy life by year 15, and {{< var wishonia_personal_upside_blend >}} in blended upside. |
| **Collapse Tail** | The destructive economy reaches 35% of GDP (Terminal Parasitic Load) by {{< var destructive_economy_35pct_year >}} if the loop is not broken. Historical collapses (Soviet Union, Yugoslavia, Argentina, Zimbabwe) consistently begin at 35-45% total extraction. |

## Personal Scenario Calculator

<style>
.pbc-shell {
  --pbc-bg: #f5efe4;
  --pbc-card: #fffaf1;
  --pbc-line: rgba(60, 43, 16, 0.16);
  --pbc-text: #2c2115;
  --pbc-muted: #6a5a45;
  --pbc-accent: #a04f1a;
  --pbc-accent-soft: rgba(160, 79, 26, 0.12);
  --pbc-good: #0e5f47;
  --pbc-bad: #8d2f25;
  background: linear-gradient(135deg, #fbf5ea 0%, #f1e4cb 100%);
  border: 1px solid var(--pbc-line);
  border-radius: 22px;
  color: var(--pbc-text);
  margin: 1.5rem 0 2rem;
  padding: clamp(1rem, 2.5vw, 1.4rem);
}

.pbc-shell h3,
.pbc-shell h4,
.pbc-shell p,
.pbc-shell label,
.pbc-shell div,
.pbc-shell span {
  color: inherit;
}

.pbc-grid {
  display: grid;
  gap: 1rem;
}

.pbc-controls {
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
}

.pbc-group {
  background: var(--pbc-card);
  border: 1px solid var(--pbc-line);
  border-radius: 16px;
  padding: 0.95rem;
}

.pbc-group label {
  display: block;
  font-size: 0.92rem;
  font-weight: 600;
  margin-bottom: 0.45rem;
}

.pbc-group input,
.pbc-group select {
  background: #fff;
  border: 1px solid rgba(60, 43, 16, 0.25);
  border-radius: 10px;
  box-sizing: border-box;
  font-size: 1rem;
  padding: 0.7rem 0.8rem;
  width: 100%;
}

.pbc-helper {
  color: var(--pbc-muted);
  font-size: 0.84rem;
  margin-top: 0.45rem;
}

.pbc-actions {
  align-items: center;
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem;
}

.pbc-button {
  background: var(--pbc-text);
  border: 0;
  border-radius: 999px;
  color: #fff;
  cursor: pointer;
  font-size: 0.92rem;
  font-weight: 600;
  padding: 0.72rem 1rem;
}

.pbc-cards {
  display: grid;
  gap: 1rem;
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
}

.pbc-card {
  background: var(--pbc-card);
  border: 1px solid var(--pbc-line);
  border-radius: 16px;
  padding: 1rem;
}

.pbc-card-title {
  color: var(--pbc-muted);
  font-size: 0.84rem;
  letter-spacing: 0.03em;
  margin-bottom: 0.35rem;
  text-transform: uppercase;
}

.pbc-card-value {
  font-size: clamp(1.35rem, 2vw, 1.55rem);
  font-weight: 700;
  line-height: 1.1;
}

.pbc-card-meta {
  color: var(--pbc-muted);
  font-size: 0.88rem;
  line-height: 1.45;
  margin-top: 0.35rem;
}

.pbc-card.good .pbc-card-value {
  color: var(--pbc-good);
}

.pbc-card.bad .pbc-card-value {
  color: var(--pbc-bad);
}

.pbc-summary {
  background: var(--pbc-card);
  border: 1px solid var(--pbc-line);
  border-radius: 16px;
  font-size: 1rem;
  line-height: 1.65;
  padding: 1rem;
}

.pbc-footnote {
  color: var(--pbc-muted);
  font-size: 0.86rem;
  line-height: 1.5;
  margin: 0;
}

.pbc-inline-value {
  font-weight: 700;
}

.visually-hidden {
  left: -9999px;
  position: absolute;
}
</style>

<div id="personal-benefits-calculator" class="pbc-shell">
  <span id="pbc-src-income" class="visually-hidden">{{< var global_avg_income_2025 >}}</span>
  <span id="pbc-src-years" class="visually-hidden">{{< var global_avg_remaining_years >}}</span>
  <span id="pbc-src-age" class="visually-hidden">{{< var global_median_age_2024 >}}</span>
  <span id="pbc-src-savings" class="visually-hidden">{{< var global_annual_savings_per_capita >}}</span>
  <span id="pbc-src-treaty-income" class="visually-hidden">{{< var treaty_trajectory_lifetime_income_gain_per_capita >}}</span>
  <span id="pbc-src-wish-income" class="visually-hidden">{{< var wishonia_trajectory_lifetime_income_gain_per_capita >}}</span>
  <span id="pbc-src-treaty-health" class="visually-hidden">{{< var treaty_hale_value_per_capita >}}</span>
  <span id="pbc-src-wish-health" class="visually-hidden">{{< var wishonia_hale_value_per_capita >}}</span>
  <span id="pbc-src-destruct-now" class="visually-hidden">{{< var global_destructive_economy_pct_gdp >}}</span>
  <span id="pbc-src-destruct-25" class="visually-hidden">{{< var destructive_economy_25pct_year >}}</span>
  <span id="pbc-src-destruct-35" class="visually-hidden">{{< var destructive_economy_35pct_year >}}</span>
  <span id="pbc-src-destruct-50" class="visually-hidden">{{< var destructive_economy_50pct_year >}}</span>

  <div class="pbc-grid">
    <div>
      <h3>How much is it rational for you to contribute?</h3>
      <p class="pbc-footnote">This calculator scales the repo's average-person scenario gains to your age, income, and available savings. It is not trying to be conservative. It is trying to show the model's full upside and the downside of staying on the current path.</p>
    </div>

    <div class="pbc-controls">
      <div class="pbc-group">
        <label for="pbc-input-scenario">Scenario upside you are trying to buy</label>
        <select id="pbc-input-scenario">
          <option value="treaty">Minimum Sustainable Trajectory (1% Treaty)</option>
          <option value="wishonia">Optimal Governance Trajectory</option>
        </select>
        <div class="pbc-helper">Collapse-tail markers are shown in the summary below as the downside of not breaking the current loop.</div>
      </div>

      <div class="pbc-group">
        <label for="pbc-input-age">Your age</label>
        <input id="pbc-input-age" type="number" min="0" max="120" step="1" inputmode="numeric">
        <div class="pbc-helper">Default = global median age.</div>
      </div>

      <div class="pbc-group">
        <label for="pbc-input-income">Your annual income</label>
        <input id="pbc-input-income" type="number" min="1" step="1000" inputmode="numeric">
        <div class="pbc-helper">Income upside scales from the global average baseline.</div>
      </div>

      <div class="pbc-group">
        <label for="pbc-input-savings">Your available savings</label>
        <input id="pbc-input-savings" type="number" min="1" step="100" inputmode="numeric">
        <div class="pbc-helper">Default = average annual savings capacity per person.</div>
      </div>

      <div class="pbc-group">
        <label for="pbc-input-contribution">Contribution you are considering</label>
        <input id="pbc-input-contribution" type="number" min="1" step="10" inputmode="numeric">
        <div class="pbc-helper">Used to compute break-even probability lift and share of savings.</div>
      </div>

      <div class="pbc-group">
        <label for="pbc-input-probability">Probability lift your contribution causes (percentage points)</label>
        <input id="pbc-input-probability" type="number" min="0.000001" step="0.0001" inputmode="decimal">
        <div class="pbc-helper">Example: <strong>0.01</strong> means one-hundredth of one percentage point.</div>
      </div>
    </div>

    <div class="pbc-actions">
      <button id="pbc-reset" class="pbc-button" type="button">Reset to the average Earth human</button>
      <div class="pbc-helper">Default contribution = 1% of default available savings.</div>
    </div>

    <div class="pbc-cards">
      <div class="pbc-card good">
        <div class="pbc-card-title">Personal Income Upside</div>
        <div id="pbc-output-income" class="pbc-card-value"></div>
        <div id="pbc-output-income-meta" class="pbc-card-meta"></div>
      </div>

      <div class="pbc-card good">
        <div class="pbc-card-title">Personal Healthy-Life Upside</div>
        <div id="pbc-output-health" class="pbc-card-value"></div>
        <div id="pbc-output-health-meta" class="pbc-card-meta"></div>
      </div>

      <div class="pbc-card good">
        <div class="pbc-card-title">Blended Personal Upside</div>
        <div id="pbc-output-blend" class="pbc-card-value"></div>
        <div id="pbc-output-blend-meta" class="pbc-card-meta"></div>
      </div>

      <div class="pbc-card">
        <div class="pbc-card-title">Expected Value Of Your Contribution</div>
        <div id="pbc-output-ev" class="pbc-card-value"></div>
        <div id="pbc-output-ev-meta" class="pbc-card-meta"></div>
      </div>

      <div class="pbc-card">
        <div class="pbc-card-title">Break-Even Contribution</div>
        <div id="pbc-output-break-even" class="pbc-card-value"></div>
        <div id="pbc-output-break-even-meta" class="pbc-card-meta"></div>
      </div>

      <div class="pbc-card bad">
        <div class="pbc-card-title">Break-Even Probability Lift</div>
        <div id="pbc-output-probability" class="pbc-card-value"></div>
        <div id="pbc-output-probability-meta" class="pbc-card-meta"></div>
      </div>
    </div>

    <div id="pbc-summary" class="pbc-summary" aria-live="polite"></div>
    <p class="pbc-footnote">Blended upside = lifetime income gain + valued healthy-life gain. It is intentionally expansive rather than conservative. The scenario cards above show the downside of letting the current trajectory continue.</p>
  </div>
</div>

<script>
(function() {
  const root = document.getElementById('personal-benefits-calculator');
  if (!root || root.dataset.initialized === 'true') {
    return;
  }
  root.dataset.initialized = 'true';

  function multiplierFromSuffix(suffix) {
    const map = { K: 1e3, M: 1e6, B: 1e9, T: 1e12 };
    return map[(suffix || '').toUpperCase()] || 1;
  }

  function parsePrimary(text, fallback, allowPercent) {
    if (!text) {
      return fallback;
    }
    const clean = text.replace(/,/g, '').replace(/\u00a0/g, ' ');
    const re = allowPercent
      ? /([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)\s*(x|%|years|year)?/i
      : /\$?\s*([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)/i;
    const match = clean.match(re);
    if (!match) {
      return fallback;
    }
    const base = Number.parseFloat(match[1]);
    if (!Number.isFinite(base)) {
      return fallback;
    }
    let value = base * multiplierFromSuffix(match[2]);
    if ((match[3] || '').includes('%')) {
      value /= 100;
    }
    return value;
  }

  function parseCi(text, fallback, allowPercent) {
    if (!text) {
      return { lower: fallback, upper: fallback };
    }
    const clean = text.replace(/,/g, '').replace(/\u00a0/g, ' ');
    const re = allowPercent
      ? /95% CI:\s*([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)\s*(%|years|year|x)?\s*-\s*([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)\s*(%|years|year|x)?/i
      : /95% CI:\s*\$?([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)\s*-\s*\$?([0-9]+(?:\.[0-9]+)?)\s*([KMBT]?)/i;
    const match = clean.match(re);
    if (!match) {
      return { lower: fallback, upper: fallback };
    }
    if (allowPercent) {
      let lower = Number.parseFloat(match[1]) * multiplierFromSuffix(match[2]);
      let upper = Number.parseFloat(match[4]) * multiplierFromSuffix(match[5]);
      if ((match[3] || '').includes('%')) {
        lower /= 100;
      }
      if ((match[6] || '').includes('%')) {
        upper /= 100;
      }
      return { lower: lower, upper: upper };
    }
    return {
      lower: Number.parseFloat(match[1]) * multiplierFromSuffix(match[2]),
      upper: Number.parseFloat(match[3]) * multiplierFromSuffix(match[4])
    };
  }

  function parseMetric(id, options) {
    const el = document.getElementById(id);
    const text = el ? el.textContent : '';
    const fallback = options.fallback;
    const central = parsePrimary(text, fallback, Boolean(options.allowPercent));
    const range = parseCi(text, central, Boolean(options.allowPercent));
    return { central: central, lower: range.lower, upper: range.upper };
  }

  function clamp(value, min, max) {
    return Math.min(Math.max(value, min), max);
  }

  function formatCurrency(value) {
    return new Intl.NumberFormat('en-US', {
      style: 'currency',
      currency: 'USD',
      maximumFractionDigits: 0
    }).format(value);
  }

  function formatRange(lower, upper) {
    return `${formatCurrency(lower)} to ${formatCurrency(upper)}`;
  }

  function formatPctPoints(value) {
    if (!Number.isFinite(value)) {
      return 'n/a';
    }
    if (value < 0.001) {
      return `${value.toFixed(6)} percentage points`;
    }
    if (value < 0.1) {
      return `${value.toFixed(4)} percentage points`;
    }
    if (value < 1) {
      return `${value.toFixed(3)} percentage points`;
    }
    return `${value.toFixed(2)} percentage points`;
  }

  function formatPercent(value) {
    if (!Number.isFinite(value)) {
      return 'n/a';
    }
    return `${(value * 100).toFixed(value < 0.01 ? 2 : 1)}%`;
  }

  const globalIncome = parseMetric('pbc-src-income', { fallback: 14400 });
  const globalYears = parseMetric('pbc-src-years', { fallback: 48.5, allowPercent: true });
  const globalAge = parseMetric('pbc-src-age', { fallback: 30.5, allowPercent: true });
  const globalSavings = parseMetric('pbc-src-savings', { fallback: 3900 });
  const treatyIncome = parseMetric('pbc-src-treaty-income', { fallback: 14900000 });
  const wishIncome = parseMetric('pbc-src-wish-income', { fallback: 52100000 });
  const treatyHealth = parseMetric('pbc-src-treaty-health', { fallback: 982500 });
  const wishHealth = parseMetric('pbc-src-wish-health', { fallback: 2355000 });
  const destructNow = parseMetric('pbc-src-destruct-now', { fallback: 0.115, allowPercent: true });
  const destruct25 = parseMetric('pbc-src-destruct-25', { fallback: 2033, allowPercent: true });
  const destruct35 = parseMetric('pbc-src-destruct-35', { fallback: 2037, allowPercent: true });
  const destruct50 = parseMetric('pbc-src-destruct-50', { fallback: 2040, allowPercent: true });

  const scenarioMap = {
    treaty: {
      label: 'Minimum Sustainable Trajectory ([1% Treaty](../solution/1-percent-treaty.qmd) [@1-pct-treaty-impact-paper-2025])',
      income: treatyIncome,
      health: treatyHealth
    },
    wishonia: {
      label: 'Optimal Governance Trajectory',
      income: wishIncome,
      health: wishHealth
    }
  };

  const scenarioInput = document.getElementById('pbc-input-scenario');
  const ageInput = document.getElementById('pbc-input-age');
  const incomeInput = document.getElementById('pbc-input-income');
  const savingsInput = document.getElementById('pbc-input-savings');
  const contributionInput = document.getElementById('pbc-input-contribution');
  const probabilityInput = document.getElementById('pbc-input-probability');
  const resetButton = document.getElementById('pbc-reset');

  function resetDefaults() {
    ageInput.value = Math.round(globalAge.central);
    incomeInput.value = Math.round(globalIncome.central);
    savingsInput.value = Math.round(globalSavings.central);
    contributionInput.value = Math.max(1, Math.round(globalSavings.central * 0.01));
    probabilityInput.value = '0.01';
    scenarioInput.value = 'treaty';
  }

  function scaledMetric(metric, scale) {
    return {
      central: metric.central * scale,
      lower: metric.lower * scale,
      upper: metric.upper * scale
    };
  }

  function setOutput(id, value) {
    const el = document.getElementById(id);
    if (el) {
      el.textContent = value;
    }
  }

  function render() {
    const scenario = scenarioMap[scenarioInput.value] || scenarioMap.treaty;
    const age = clamp(Number.parseFloat(ageInput.value) || globalAge.central, 0, 120);
    const income = Math.max(1, Number.parseFloat(incomeInput.value) || globalIncome.central);
    const savings = Math.max(1, Number.parseFloat(savingsInput.value) || globalSavings.central);
    const contribution = Math.max(1, Number.parseFloat(contributionInput.value) || 1);
    const probabilityPoints = Math.max(0, Number.parseFloat(probabilityInput.value) || 0);
    const probabilityFraction = probabilityPoints / 100;

    const remainingYears = Math.max(1, globalYears.central + (globalAge.central - age));
    const incomeScale = income / globalIncome.central;
    const timeScale = remainingYears / globalYears.central;

    const personalIncome = scaledMetric(scenario.income, incomeScale * timeScale);
    const personalHealth = scaledMetric(scenario.health, timeScale);
    const personalBlend = {
      central: personalIncome.central + personalHealth.central,
      lower: personalIncome.lower + personalHealth.lower,
      upper: personalIncome.upper + personalHealth.upper
    };

    const evIncome = scaledMetric(personalIncome, probabilityFraction);
    const evBlend = scaledMetric(personalBlend, probabilityFraction);
    const breakEvenProbabilityIncome = contribution / Math.max(personalIncome.central, 1) * 100;
    const breakEvenProbabilityBlend = contribution / Math.max(personalBlend.central, 1) * 100;
    const savingsShare = contribution / savings;

    setOutput('pbc-output-income', formatCurrency(personalIncome.central));
    setOutput('pbc-output-income-meta', `Range: ${formatRange(personalIncome.lower, personalIncome.upper)}`);

    setOutput('pbc-output-health', formatCurrency(personalHealth.central));
    setOutput('pbc-output-health-meta', `Range: ${formatRange(personalHealth.lower, personalHealth.upper)}`);

    setOutput('pbc-output-blend', formatCurrency(personalBlend.central));
    setOutput('pbc-output-blend-meta', `Range: ${formatRange(personalBlend.lower, personalBlend.upper)}`);

    setOutput('pbc-output-ev', formatCurrency(evBlend.central));
    setOutput('pbc-output-ev-meta', `Income-only EV: ${formatCurrency(evIncome.central)}. Blended range: ${formatRange(evBlend.lower, evBlend.upper)}`);

    setOutput('pbc-output-break-even', formatCurrency(evBlend.central));
    setOutput('pbc-output-break-even-meta', `At ${formatPctPoints(probabilityPoints)}, income-only break-even is ${formatCurrency(evIncome.central)}.`);

    setOutput('pbc-output-probability', formatPctPoints(breakEvenProbabilityBlend));
    setOutput('pbc-output-probability-meta', `Income-only break-even: ${formatPctPoints(breakEvenProbabilityIncome)}.`);

    setOutput(
      'pbc-summary',
      `At age ${age}, with ${formatCurrency(income)} annual income and ${formatCurrency(savings)} available savings, the ${scenario.label} implies ${formatCurrency(personalBlend.central)} in blended upside for you personally. A ${formatCurrency(contribution)} contribution is ${formatPercent(savingsShare)} of your available savings. If you believe that contribution raises success probability by ${formatPctPoints(probabilityPoints)}, its blended expected value is ${formatCurrency(evBlend.central)}. Meanwhile, the downside of not breaking the current loop is a destructive economy already at ${formatPercent(destructNow.central)} of GDP, reaching 35% (Terminal Parasitic Load) by ${destruct35.central}. Historical collapses consistently begin at 35-45% total extraction.`
    );
  }

  resetButton.addEventListener('click', function() {
    resetDefaults();
    render();
  });

  [scenarioInput, ageInput, incomeInput, savingsInput, contributionInput, probabilityInput].forEach(function(el) {
    el.addEventListener('input', render);
  });

  resetDefaults();
  render();
})();
</script>

## What This Means

If the calculator says a tiny probability shift justifies a meaningful contribution, that is not because the calculator is being sentimental. It is because the spread between the current trajectory and the better trajectories is enormous.

If you want the mechanism that tries to buy those better futures, read [The Earth Optimization Prize](../strategy/earth-optimization-prize.qmd). If you want the short version, do the three actions: make a PRIZE deposit, answer the referendum questions, and recruit two people. For the "recruit two people" part, the exact message to send and the twenty-two-question phone script to walk them through is at [The Horizontal Path](../appendix/recruitment-and-propaganda-plan.qmd#the-horizontal-path).

---

*For the full parameter set, Monte Carlo outputs, and sensitivity analysis, see the [Parameters and Calculations](../appendix/parameters-and-calculations.qmd) chapter.*
